# Trace continuity across an `.Async()` actor boundary

A traced source pushes one element while a `consume` span is current. The stream crosses an `.Async()` boundary — a publisher/subscriber actor hop — between two `Select` stages. The producer's `ActivityContext` is captured at the `ActorOutputBoundary`, carried on the boundary's `OnNext` event, and re-armed on the downstream `BatchingActorInputBoundary`, so the post-boundary `Select` and the terminal `Seq` sink stay on the SAME TraceId as the pre-boundary stage. Before #8243 the actor hop dropped the context and the downstream stages started a fresh, disconnected trace.

This rendering was generated by running `StreamsTraceRenderingSpec.Render_async_boundary_continuity` against a live `GraphInterpreter` — the span data below is what the real interpreter emitted, not a hand-drawn mock.

## Captured spans

```
├─ akka.stream.stage Select
│     trace=9d9e36e5b9de8e13450181874cc03dbd span=ed5549145af4e7cb
  ├─ akka.stream.stage Select
  │     trace=9d9e36e5b9de8e13450181874cc03dbd span=33553476e4a99887
    ├─ akka.stream.stage SeqStage
    │     trace=9d9e36e5b9de8e13450181874cc03dbd span=f898481b6a477316
```

## Distinct trace ids: 1

- `9d9e36e5b9de8e13450181874cc03dbd`

