<Project>
  <!-- use the xunit JSON settings file -->
  <ItemGroup>
    <Content Include="$(SolutionDir)src/xunit.multinode.runner.json" Link="xunit.multinode.runner.json">
      <CopyToOutputDirectory>Always</CopyToOutputDirectory>
    </Content>
  </ItemGroup>
</Project>