// <auto-generated />
#nullable enable
using System;
using System.Buffers;

namespace MarkedUnionGolden.AssemblyB;

public sealed partial class EnvelopeSerializer
{
    public EnvelopeSerializer(global::Akka.Actor.ExtendedActorSystem system) : base(system)
    {
    }

    public override int Identifier => 190102;

    public static partial global::Akka.Serialization.V2.SerializerRegistration CreateRegistration()
    {
        return global::Akka.Serialization.V2.SerializerRegistration.Create("marked-union-golden",
            system => new EnvelopeSerializer(system),
            global::System.Collections.Immutable.ImmutableHashSet.Create<global::System.Type>(
                typeof(global::MarkedUnionGolden.AssemblyB.IEnvelopeProtocol))); 
    }

    public override string Manifest(object obj)
    {
        return obj switch
        {
            global::MarkedUnionGolden.AssemblyB.Envelope => "envelope-v1",
            _ => throw new global::System.ArgumentException($"Unsupported generated serializer type '{obj.GetType()}' from assembly '{obj.GetType().Assembly.GetName().Name}'. This serializer was generated in assembly 'AkkaSerializationGeneratorHarness', which does not recognize this type.", nameof(obj))
        };
    }

    public override int Serialize(object obj, IBufferWriter<byte> writer)
    {
        var countingWriter = new AkkaGeneratedCountingBufferWriter(writer);
        var messagePackWriter = new global::MessagePack.MessagePackWriter(countingWriter);
        SerializeMessagePack(obj, ref messagePackWriter);
        messagePackWriter.Flush();
        return checked((int)countingWriter.BytesWritten);
    }

    private void SerializeMessagePack(object obj, ref global::MessagePack.MessagePackWriter writer)
    {
        switch (obj)
        {
            case global::MarkedUnionGolden.AssemblyB.Envelope message:
                WriteEnvelope(ref writer, message);
                break;
            default:
                throw new global::System.ArgumentException($"Unsupported generated serializer type '{obj.GetType()}' from assembly '{obj.GetType().Assembly.GetName().Name}'. This serializer was generated in assembly 'AkkaSerializationGeneratorHarness', which does not recognize this type.", nameof(obj));
        }
    }

    public override object Deserialize(ReadOnlySequence<byte> bytes, string manifest)
    {
        var reader = new global::MessagePack.MessagePackReader(bytes);
        return manifest switch
        {
            "envelope-v1" => ReadEnvelope(ref reader),
            _ => throw new global::System.Runtime.Serialization.SerializationException($"Unknown generated serializer manifest [{manifest}] for serializer [{GetType()}].")
        };
    }

    public override int SizeHint(object obj)
    {
        return obj switch
        {
            global::MarkedUnionGolden.AssemblyB.Envelope message => SizeOfEnvelope(message),
            _ => global::Akka.Serialization.SerializerV2.UnknownSize
        };
    }

    private sealed class AkkaGeneratedCountingBufferWriter : global::System.Buffers.IBufferWriter<byte>
    {
        private readonly global::System.Buffers.IBufferWriter<byte> _inner;

        public AkkaGeneratedCountingBufferWriter(global::System.Buffers.IBufferWriter<byte> inner)
        {
            _inner = inner;
        }

        public long BytesWritten { get; private set; }

        public void Advance(int count)
        {
            _inner.Advance(count);
            BytesWritten += count;
        }

        public global::System.Memory<byte> GetMemory(int sizeHint = 0)
        {
            return _inner.GetMemory(sizeHint);
        }

        public global::System.Span<byte> GetSpan(int sizeHint = 0)
        {
            return _inner.GetSpan(sizeHint);
        }
    }

    private int SizeOfEnvelope(global::MarkedUnionGolden.AssemblyB.Envelope message)
    {
        checked
        {
            var size = SizeOfMapHeader(1);
            size += SizeOfInt32(1);
            var destinationSize = SizeOfUnion_IDestination(message.Destination);
            if (destinationSize < 0)
                return global::Akka.Serialization.SerializerV2.UnknownSize;
            size += destinationSize;
            return size;
        }
    }

    private void WriteEnvelope(ref global::MessagePack.MessagePackWriter writer, global::MarkedUnionGolden.AssemblyB.Envelope message)
    {
        writer.WriteMapHeader(1);
        writer.Write(1);
        WriteUnion_IDestination(ref writer, message.Destination);
    }

    private global::MarkedUnionGolden.AssemblyB.Envelope ReadEnvelope(ref global::MessagePack.MessagePackReader reader)
    {
        var __fieldCount = reader.ReadMapHeader();
        global::MarkedUnionGolden.AssemblyA.IDestination? destination = null;
        var __hasDestination = false;
        for (var __entryIndex = 0; __entryIndex < __fieldCount; __entryIndex++)
        {
            var __fieldId = reader.ReadInt32();
            switch (__fieldId)
            {
                case 1:
                    destination = ReadUnion_IDestination(ref reader);
                    __hasDestination = true;
                    break;
                default:
                    reader.Skip();
                    break;
            }
        }

        if (!__hasDestination || destination is null)
            throw new global::System.Runtime.Serialization.SerializationException("Missing required field [Destination] with index [1] while deserializing [global::MarkedUnionGolden.AssemblyB.Envelope].");
        return new global::MarkedUnionGolden.AssemblyB.Envelope(Destination: destination!);
    }

    private int SizeOfTechDestination(global::MarkedUnionGolden.AssemblyB.TechDestination message)
    {
        checked
        {
            var size = SizeOfMapHeader(1);
            size += SizeOfInt32(1);
            size += SizeOfString(message.Cluster);
            return size;
        }
    }

    private void WriteTechDestination(ref global::MessagePack.MessagePackWriter writer, global::MarkedUnionGolden.AssemblyB.TechDestination message)
    {
        writer.WriteMapHeader(1);
        writer.Write(1);
        writer.Write(message.Cluster);
    }

    private global::MarkedUnionGolden.AssemblyB.TechDestination ReadTechDestination(ref global::MessagePack.MessagePackReader reader)
    {
        var __fieldCount = reader.ReadMapHeader();
        string? cluster = null;
        var __hasCluster = false;
        for (var __entryIndex = 0; __entryIndex < __fieldCount; __entryIndex++)
        {
            var __fieldId = reader.ReadInt32();
            switch (__fieldId)
            {
                case 1:
                    cluster = reader.ReadString();
                    __hasCluster = true;
                    break;
                default:
                    reader.Skip();
                    break;
            }
        }

        if (!__hasCluster || cluster is null)
            throw new global::System.Runtime.Serialization.SerializationException("Missing required field [Cluster] with index [1] while deserializing [global::MarkedUnionGolden.AssemblyB.TechDestination].");
        return new global::MarkedUnionGolden.AssemblyB.TechDestination(Cluster: cluster!);
    }

    private int SizeOfCommsDestination(global::MarkedUnionGolden.AssemblyA.CommsDestination message)
    {
        checked
        {
            var size = SizeOfMapHeader(1);
            size += SizeOfInt32(1);
            size += SizeOfString(message.Host);
            return size;
        }
    }

    private void WriteCommsDestination(ref global::MessagePack.MessagePackWriter writer, global::MarkedUnionGolden.AssemblyA.CommsDestination message)
    {
        writer.WriteMapHeader(1);
        writer.Write(1);
        writer.Write(message.Host);
    }

    private global::MarkedUnionGolden.AssemblyA.CommsDestination ReadCommsDestination(ref global::MessagePack.MessagePackReader reader)
    {
        var __fieldCount = reader.ReadMapHeader();
        string? host = null;
        var __hasHost = false;
        for (var __entryIndex = 0; __entryIndex < __fieldCount; __entryIndex++)
        {
            var __fieldId = reader.ReadInt32();
            switch (__fieldId)
            {
                case 1:
                    host = reader.ReadString();
                    __hasHost = true;
                    break;
                default:
                    reader.Skip();
                    break;
            }
        }

        if (!__hasHost || host is null)
            throw new global::System.Runtime.Serialization.SerializationException("Missing required field [Host] with index [1] while deserializing [global::MarkedUnionGolden.AssemblyA.CommsDestination].");
        return new global::MarkedUnionGolden.AssemblyA.CommsDestination(Host: host!);
    }

    private void WriteUnion_IDestination(ref global::MessagePack.MessagePackWriter writer, global::MarkedUnionGolden.AssemblyA.IDestination value)
    {
        var runtimeType = value.GetType();
        if (runtimeType == typeof(global::MarkedUnionGolden.AssemblyB.TechDestination))
        {
            writer.WriteMapHeader(2);
            writer.Write(1);
            writer.Write("dest-tech-v1");
            writer.Write(2);
            WriteTechDestination(ref writer, (global::MarkedUnionGolden.AssemblyB.TechDestination)value);
            return;
        }

        if (runtimeType == typeof(global::MarkedUnionGolden.AssemblyA.CommsDestination))
        {
            writer.WriteMapHeader(2);
            writer.Write(1);
            writer.Write("dest-comms-v1");
            writer.Write(2);
            WriteCommsDestination(ref writer, (global::MarkedUnionGolden.AssemblyA.CommsDestination)value);
            return;
        }

        throw new global::System.Runtime.Serialization.SerializationException($"Type [{runtimeType}] is not a declared union member for union [global::MarkedUnionGolden.AssemblyA.IDestination].");
    }

    private global::MarkedUnionGolden.AssemblyA.IDestination ReadUnion_IDestination(ref global::MessagePack.MessagePackReader reader)
    {
        var fieldCount = reader.ReadMapHeader();
        string? manifest = null;
        global::MarkedUnionGolden.AssemblyA.IDestination? result = default;
        var hasPayload = false;
        for (var entryIndex = 0; entryIndex < fieldCount; entryIndex++)
        {
            var fieldId = reader.ReadInt32();
            switch (fieldId)
            {
                case 1:
                    manifest = reader.ReadString();
                    break;
                case 2:
                    switch (manifest)
                    {
                        case "dest-tech-v1":
                            result = ReadTechDestination(ref reader);
                            break;
                        case "dest-comms-v1":
                            result = ReadCommsDestination(ref reader);
                            break;
                        case null:
                            throw new global::System.Runtime.Serialization.SerializationException("Union manifest must precede the payload for union [global::MarkedUnionGolden.AssemblyA.IDestination].");
                        default:
                            throw new global::System.Runtime.Serialization.SerializationException($"Unknown union manifest [{manifest}] for union [global::MarkedUnionGolden.AssemblyA.IDestination].");
                    }

                    hasPayload = true;
                    break;
                default:
                    reader.Skip();
                    break;
            }
        }

        if (!hasPayload || result is null)
            throw new global::System.Runtime.Serialization.SerializationException("Missing union payload for union [global::MarkedUnionGolden.AssemblyA.IDestination].");
        return result;
    }

    private int SizeOfUnion_IDestination(global::MarkedUnionGolden.AssemblyA.IDestination value)
    {
        var runtimeType = value.GetType();
        if (runtimeType == typeof(global::MarkedUnionGolden.AssemblyB.TechDestination))
        {
            var payloadSize = SizeOfTechDestination((global::MarkedUnionGolden.AssemblyB.TechDestination)value);
            if (payloadSize < 0)
                return global::Akka.Serialization.SerializerV2.UnknownSize;
            return checked(SizeOfMapHeader(2) + SizeOfInt32(1) + SizeOfString("dest-tech-v1") + SizeOfInt32(2) + payloadSize);
        }

        if (runtimeType == typeof(global::MarkedUnionGolden.AssemblyA.CommsDestination))
        {
            var payloadSize = SizeOfCommsDestination((global::MarkedUnionGolden.AssemblyA.CommsDestination)value);
            if (payloadSize < 0)
                return global::Akka.Serialization.SerializerV2.UnknownSize;
            return checked(SizeOfMapHeader(2) + SizeOfInt32(1) + SizeOfString("dest-comms-v1") + SizeOfInt32(2) + payloadSize);
        }

        return global::Akka.Serialization.SerializerV2.UnknownSize;
    }

}
